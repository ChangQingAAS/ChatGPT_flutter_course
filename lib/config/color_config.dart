import 'package:flutter/material.dart';

/// class [ColorConfig] contains all the colors used in the app.
class ColorConfig {
  /// scaffoldBackgroundColor is the background color of the scaffold.
  static Color scaffoldBackgroundColor = const Color(0xFF343541);

  /// cardColor is the background color of the card.
  static Color cardColor = const Color(0xFF444654);
}
